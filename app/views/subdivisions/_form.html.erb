<%= form_with(model: subdivision) do |form| %>
  <% if subdivision.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subdivision.errors.count, "error") %> prohibited this subdivision from being saved:</h2>

      <ul>
        <% subdivision.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :city, style: "display: block" %>
    <%= form.text_field :city %>
  </div>

  <div>
    <%= form.label :postal_code, style: "display: block" %>
    <%= form.text_field :postal_code %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
