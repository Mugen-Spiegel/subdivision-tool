<%= form_with(model: water_billing, url: subdivision_water_billings_path) do |form| %>
  <% if water_billing.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(water_billing.errors.count, "error") %> prohibited this water_billing from being saved:</h2>

      <ul>
        <% water_billing.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :per_cubic_price, style: "display: block" %>
    <%= form.text_field :per_cubic_price %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
