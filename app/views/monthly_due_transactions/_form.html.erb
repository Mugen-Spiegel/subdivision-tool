<%= form_with(model: monthly_due_transaction) do |form| %>
  <% if monthly_due_transaction.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(monthly_due_transaction.errors.count, "error") %> prohibited this monthly_due_transaction from being saved:</h2>

      <ul>
        <% monthly_due_transaction.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :monthly_due_id, style: "display: block" %>
    <%= form.text_field :monthly_due_id %>
  </div>

  <div>
    <%= form.label :subdivision_id, style: "display: block" %>
    <%= form.text_field :subdivision_id %>
  </div>

  <div>
    <%= form.label :is_paid, style: "display: block" %>
    <%= form.text_field :is_paid %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
