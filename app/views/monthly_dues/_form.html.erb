<%= form_with(model: monthly_due) do |form| %>
  <% if monthly_due.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(monthly_due.errors.count, "error") %> prohibited this monthly_due from being saved:</h2>

      <ul>
        <% monthly_due.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :amount, style: "display: block" %>
    <%= form.text_field :amount %>
  </div>

  <div>
    <%= form.label :subdivision_id, style: "display: block" %>
    <%= form.text_field :subdivision_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
